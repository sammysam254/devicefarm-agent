# Tray Icon Placeholder

Place your custom `icon.png` file in this directory.

**Requirements:**
- Format: PNG
- Recommended size: 256×256 pixels (Electron will scale it down for the system tray)
- Transparency supported
- The file MUST be named `icon.png`

The Electron tray will load this icon automatically on startup.
