name: Release Setup Script

on:
  push:
    branches: [ main ]
    paths: [ 'DeviceFarm-Agent-Setup.bat' ]
  workflow_dispatch:

jobs:
  release:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Get version from package.json
        id: version
        run: |
          $pkg = Get-Content package.json | ConvertFrom-Json
          echo "version=$($pkg.version)" >> $env:GITHUB_OUTPUT
        shell: pwsh

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: DeviceFarm-Agent-Setup.bat
          tag_name: v${{ steps.version.outputs.version }}
          name: v${{ steps.version.outputs.version }} - Setup Script
          body: |
            Download **DeviceFarm-Agent-Setup.bat** and run it to install the DeviceFarm Agent.
            
            The setup script will:
            - Install Git, Node.js, and ADB if missing
            - Clone the agent repository
            - Install all dependencies
            - Launch the agent
            
            [Documentation](https://github.com/sammysam254/devicefarm-agent)
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
